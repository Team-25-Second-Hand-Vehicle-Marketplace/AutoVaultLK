/** all-MiniLM-L6-v2 output size — must match marketplace.vehicles.embedding vector(384). */
export const EMBEDDING_DIMENSIONS = 384;

/** Hugging Face / Xenova id. Ingestion and search must use this exact string (FR-22.1). */
export const EMBEDDING_MODEL_ID = 'Xenova/all-MiniLM-L6-v2';
